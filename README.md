# Ruby Hospital Updates

Public binary/update repository for the Ruby Hospital application.

This repository intentionally contains only approved release binaries and update metadata. Source code, DevExpress build dependencies, credentials, customer configuration, and private build assets must remain in the private `asapamit/Ruby-Hospital` repository.

The application checks the stable manifest at `latest.json`. Versioned application update packages are published as GitHub Release assets.
